const argParser = require("../../../argParser")

module.exports = (
    /**
      * @returns {{path: string}}
     */
    () => {
        let res = {
            path: `/server-info/ping`
        }

        return res
    })